<Page
    x:Class="universal_windows_platform_cs.Views.ProductAddPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    Style="{StaticResource PageStyle}"
    mc:Ignorable="d">
    <Grid x:Name="ContentArea" Margin="{StaticResource MediumLeftRightMargin}">
        <Grid
            Background="{ThemeResource SystemControlPageBackgroundChromeLowBrush}">
            <!--
                The SystemControlPageBackgroundChromeLowBrush background represents where you should place your content. 
                Place your content here.
            -->
            <RelativePanel
                Margin="{StaticResource MarginHeader}">
                <StackPanel x:Name="NewProduct">
                    <TextBlock>ProductName</TextBlock>
                    <TextBox
            x:Name="ProductName"
            TextChanged="ProductNameText"></TextBox>
                    <TextBlock>Total</TextBlock>
                    <TextBox
            x:Name="Total"
            TextChanged="TotalText"></TextBox>
                    <Button
            Command="{x:Bind ViewModel.CreateProductCommand}">
                        Create
                    </Button>
                </StackPanel>
            </RelativePanel>
        </Grid>
    </Grid>
</Page>
